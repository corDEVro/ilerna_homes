RewriteEngine On

# No reescribir si el archivo o directorio existe
RewriteCond %{REQUEST_FILENAME} -f [OR]
RewriteCond %{REQUEST_FILENAME} -d
RewriteRule ^ - [L]

# Redirigir todo lo demas al router
RewriteRule ^(.*)$ index.php [QSA,L]
